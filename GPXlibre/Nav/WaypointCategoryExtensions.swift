import Foundation

/// Catégories ajoutées pour le signalement 1-tap Mode Nav (Bloc 2) — local uniquement,
/// pas de serveur de partage en v1 (viendra plus tard, communautaire).
extension WaypointCategory {
    static let navReportCategories: [WaypointCategory] = [.danger, .bouchon, .attention]
}
